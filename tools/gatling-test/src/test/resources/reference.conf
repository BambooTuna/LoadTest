gatling {

  endpoints {
    url = "http://localhost:8080"
    url = ${?GATLING_BASE_URL}
  }

  users = 1000
  users = ${?MR_GATLING_USERS}
  ramp-duration = 10s
  ramp-duration = ${?MR_GATLING_RAMP_DURATION}
  hold-duration = 30s
  hold-duration = ${?MR_GATLING_HOLD_DURATION}

}