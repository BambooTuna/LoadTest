apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: loadtest
spec:
  replicas: 2
  template:
    metadata:
      labels:
        run: loadtest
    spec:
      containers:
        - name: loadtest
          image: DOCKER_TAG
          ports:
            - containerPort: 80